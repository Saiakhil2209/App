import 'package:flutter/material.dart';
class PendingVouchScreen extends StatelessWidget {
  const PendingVouchScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Pending')), body: const Center(child: Text('Pending vouch screen')));
  }
}
